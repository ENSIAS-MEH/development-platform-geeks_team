apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-zookeeper
  namespace: devconnect
  labels:
    app: event-zookeeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-zookeeper
  template:
    metadata:
      labels:
        app: event-zookeeper
    spec:
      containers:
        - name: zookeeper
          image: confluentinc/cp-zookeeper:7.5.0
          env:
            - name: ZOOKEEPER_CLIENT_PORT
              value: "2181"
            - name: ZOOKEEPER_TICK_TIME
              value: "2000"
          ports:
            - containerPort: 2181
---
apiVersion: v1
kind: Service
metadata:
  name: event-zookeeper
  namespace: devconnect
spec:
  ports:
    - port: 2181
      targetPort: 2181
  selector:
    app: event-zookeeper
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-kafka
  namespace: devconnect
  labels:
    app: event-kafka
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-kafka
  template:
    metadata:
      labels:
        app: event-kafka
    spec:
      containers:
        - name: kafka
          image: confluentinc/cp-kafka:7.5.0
          env:
            - name: KAFKA_BROKER_ID
              value: "1"
            - name: KAFKA_ZOOKEEPER_CONNECT
              value: "event-zookeeper:2181"
            - name: KAFKA_ADVERTISED_LISTENERS
              value: "PLAINTEXT://event-kafka:9092"
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: "1"
            - name: KAFKA_LISTENERS
              value: "PLAINTEXT://0.0.0.0:9092"
          ports:
            - containerPort: 9092
---
apiVersion: v1
kind: Service
metadata:
  name: event-kafka
  namespace: devconnect
spec:
  ports:
    - port: 9092
      targetPort: 9092
  selector:
    app: event-kafka
