apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-username/development-platform-geeks_team.git'
    targetRevision: HEAD
    path: techhub/frontend/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: techhub
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
