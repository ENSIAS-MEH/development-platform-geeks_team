apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: event-service
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-username/development-platform-geeks_team.git'
    targetRevision: userservice/cicd
    path: techhub/event-service/k8s
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: techhub
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
