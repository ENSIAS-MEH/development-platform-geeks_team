apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: devconnect
  labels:
    app: event-service
spec:
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: event-service
  type: ClusterIP
